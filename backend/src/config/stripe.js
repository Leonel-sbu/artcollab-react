/**
 * Stripe Configuration
 * TODO: Implement when adding Stripe features beyond basic payments
 * Currently unused - stripe is initialized directly in stripeController.js
 */
module.exports = {
    // stripe: require('stripe')(process.env.STRIPE_SECRET_KEY),
};
