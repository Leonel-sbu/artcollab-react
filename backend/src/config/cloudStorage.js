/**
 * Cloud Storage Configuration
 * TODO: Implement when adding cloud storage (AWS S3, Google Cloud Storage)
 * Currently unused - local file storage via /uploads is used
 */
module.exports = {
    // bucket: process.env.GCS_BUCKET_NAME,
    // projectId: process.env.GCS_PROJECT_ID,
    // keyFilename: process.env.GCS_KEY_FILE,
};
